services:
  db:
    image: postgres:16
    container_name: knowledge-hub_db-prod
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - app-network

  backend:
    image: ghcr.io/project-construct-x/knowledge-hub-backend:latest
    container_name: knowledge-hub_backend-prod
    environment:
      APP_ENV: production
    ports:
      - "8000:8000"
    depends_on:
      - db
    restart: unless-stopped
    volumes:
      - static_images_volume:/app/static/images
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  frontend:
    image: ghcr.io/project-construct-x/knowledge-hub-frontend:latest
    container_name: knowledge-hub_frontend-prod
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db_data:
  static_images_volume:
